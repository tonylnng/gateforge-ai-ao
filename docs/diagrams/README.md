# Diagrams

Source files for diagrams referenced in `docs/`. Editable in:

- `*.mmd` — [Mermaid](https://mermaid.js.org/) (renderable in GitHub Markdown)
- `*.drawio` — [draw.io](https://drawio.com/) (export to SVG/PNG)

Render script: `tools/render-diagrams.sh` (TBD).
